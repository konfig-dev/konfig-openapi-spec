{
  "name": "konfig-openapi-spec",
  "version": "1.0.76",
  "packageManager": "yarn@3.2.3",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "dev": "nodemon --watch './src/**/*' -e ts --exec 'yarn build",
    "build": "yarn clean && tsc && yarn generate",
    "generate": "ts-node src/generate.ts",
    "generate-sdk": "yarn konfig generate",
    "generate-sdk-dev": "yarn konfig generate -d",
    "clean": "rm -rf dist/",
    "prepare": "yarn build",
    "release-ci": "release-it --ci",
    "release": "release-it"
  },
  "devDependencies": {
    "nodemon": "^2.0.20",
    "release-it": "workspace:^",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.4",
    "yaml": "^2.2.0"
  },
  "dependencies": {
    "konfig-cli": "workspace:^",
    "konfig-lib": "workspace:^",
    "konfig-zod-to-openapi": "workspace:^"
  }
}
